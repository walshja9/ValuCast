flask>=3.0,<4.0
gunicorn>=23.0,<24.0
requests>=2.31,<3.0
Pillow>=10.0,<13.0
numpy>=1.26,<3.0  # paired case-bootstrap CIs in the nightly dynasty backtest (provability batch R1/R2); seeded default_rng, reporting-only fields
anthropic>=0.39,<1.0  # ValuCast-owned scouting LLM writer (optional; flag-gated, offline-safe)
scipy>=1.11,<2.0  # ordinal calibration power analysis (prospects/ordinal_calibration_power.py, scipy.optimize.minimize)
