flask>=3.0,<4.0
gunicorn>=23.0,<24.0
requests>=2.31,<3.0
Pillow>=10.0,<13.0
qrcode>=7.4,<9.0  # share-graphic QR codes back to the live page (optional; guarded, offline-safe)
anthropic>=0.39,<1.0  # ValuCast-owned scouting LLM writer (optional; flag-gated, offline-safe)
