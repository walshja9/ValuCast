# Implementation Plans — ValuCast DD-Independence P0 Cleanup

Generated by the `improve` skill on 2026-06-17 (planned at commit `c8d8a22`).
Goal: take the "ValuCast is fully independent of Diamond Dynasties (DD)" claim from
*mostly true by architecture* to *boringly enforceable*. Each plan is
self-contained; the executor (Codex) should read the whole plan, honor its STOP
conditions, and update its status row when done. The reviewer (Fable) runs
verification and gates acceptance.

## Audit result (steps 1-2: find + classify)

DD dependencies in the public snapshot, cards, methodology copy, and pipeline,
classified per the requested taxonomy:

- **SCORE-INPUT — none.** The score path is already clean: `_score_components`
  never reads the DD row, DD is in `PROHIBITED_SCORE_INPUTS`, and the prospect
  input contract is ValuCast-owned. "Remove score dependency" is effectively done.
- **DISPLAY-FALLBACK** — `mlb_stat_line` (DD-only, empty for prospects, no owned
  source yet — `rank_v1.py:1179`) and the transitional `stat_line_translated`
  DD fallback (owned translation is primary; DD only when a prospect has zero
  owned MiLB rows — `rank_v1.py:1136`). Both already counted in
  `validation.dd_factual_fallback`.
- **COMPARISON-ONLY** — `source_ranks` (CFR/HKB/Pipeline) in a collapsed, labeled
  external-board panel (no owned equivalent; kept per product decision). The
  outbound DD 7x7 adapter is an export, not a dependency — no action.
- **DEAD-LEGACY** — unused `dd_*` rank/value keys embedded in the public snapshot
  context (no template renders them); a `value_history` sparkline render path
  wired to a DD-only field the snapshot never populates (renders nothing);
  `scripts/sync_dd_prospect_inputs.py` (off the live workflow).
- **DEPLOY COUPLING** — `VALUCAST_REQUIRE_DD` boot guard, `/health/ready` `"dd"`
  gate, freshness DD_FEED row, and the `_select_dynasty_store`/`_select_buy_source`
  fallback that silently reverts the live board to raw DD when the snapshot is
  un-ready.
- **VALIDATOR GAP** — `source_policy.dd_values_used`/`dd_ranks_used` are hardcoded
  literals; validation only checks the literal equals `False`, never derives it
  from real row sources. `factual_path_fully_valucast_owned` is reported but never
  enforced. A score silently reverting to DD passes every gate green today.

## Product decisions (Alex, 2026-06-17)

1. **Remove DD as a live valuation fallback.** Serve the last-good ValuCast
   snapshot (stale-but-valid, labeled) or an explicit unavailable state — never
   silently serve DD values. DD stays comparison-only/benchmark/internal. Deploy
   gates validate ValuCast snapshot health directly. → Plan 002.
2. **Keep the external-board context panel, comparison-only.** Collapsed by
   default, labeled "not used in ValuCast score", under display-only fields, never
   on share graphics, and validator-enforced to never influence any ValuCast
   number. → enforcement in Plan 001, label in Plan 003.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Fail-loud DD/external independence validators | P1 | M | — | DONE (Fable, 6/17; 1310 pass, ruff clean) |
| 002  | Remove DD as a live valuation fallback + retire 3 deploy gates | P1 | M | 001 | DONE (Fable, 6/17; 1311 pass, ruff clean) |
| 003  | Strip unused DD context keys from public snapshot + tighten panel label | P2 | S | — | DONE (Fable, 6/17; 1312 pass, ruff clean) |
| 004  | Pipeline honesty: season/offseason guards + gates for ungated surfaces | P1 | M | — | DONE (opus executor + Fable review, 7/8; 27 targeted pass, both validators green; also fixed a 4th hardcoded season in fetch_game_logs; freshness gate reports metadata season missing until the next daily build writes it — by design) |
| 005  | Serving cache pass: generation-keyed caches on the hot routes | P1 | M | 007 first (both edit app.py) | DONE (opus executor + Fable review, 7/8; byte-parity 8/9 exact + 1 attributed hour-badge exclusion, 1762 pass; dynasty metadata keyed on preset-not-closure; survived a mid-run API-529 resume) |
| 006  | Archive growth containment: shallow CI clones + retention decision memo | P2 | S | — | DONE (Fable direct, 7/8; fetch-depth 1 ×3, YAML valid, memo at docs/specs/2026-07-08-prediction-archive-retention.md; retention itself = plan 009 post-7/13) |
| 007  | PNG cache: thread race fix + w_/pt_ key hardening | P1 | S | — | DONE (opus executor + Fable review, 7/8; review caught a pt_ cache-poisoning hole — unknown-but-real stat suffixes now dropped at parse via `_POINT_STAT_IDS`; 154 pass) |
| 008  | Self-host web fonts; drop Google origins from CSP | P2 | S-M | — | DONE (opus executor + Fable review, 7/8; 3 variable woff2 latin subsets self-hosted incl. Archivo, CSP dropped both Google hosts, 1763 pass; visual smoke = Alex on next deploy) |

## Batch 2 — 2026-07-08 full-stack audit (planned at `72e68864`)

Plans 004–008 come from the 7/8 deep audit (9 dimensions, adversarially
verified findings). Recommended order: **007 → 005** (007 is minutes and both
touch app.py's cache region), **004** any time (pipeline files, no overlap),
**006** and **008** whenever convenient. None depend on the daily build's
timing, but every plan forbids pushing — master auto-deploys valucast.app, so
the reviewer gates all pushes.

### Findings audited 7/8 but NOT planned (so nobody re-audits them)

- **app.py monolith extraction** (share-card Pillow renderers → web/ module):
  real, L-effort, deliberately deferred — do it as the first slice of the
  incremental extraction when app.py next fights back; not before the 7/13
  ledger week.
- **CSP `unsafe-inline` in script-src**: real but decorative-CSP is
  defense-in-depth only (no XSS sink found; autoescape intact, zero |safe).
  Needs an inline-handler migration (12 scripts / 64 handlers) — plan it as
  its own project, not a batch rider.
- **Two-nav reconciliation (header vs horizon tabs)**: confirmed UX debt;
  Alex decision on the canonical nav model needed before any plan is honest.
- **Dead legacy buy-score path + its tests; dead tier-color rainbow;
  README post-processor drift (ReplacementLevel/PositionScarcity documented
  but unwired); buys dual-renderer bg/border drift; theme-token drift
  (#23252c ×13)**: all verified, all S — good rainy-day batch, none urgent.
- **19 LOW passthrough items** (unverified investigate list): recorded in the
  7/8 audit output; notable: /compare lacks the HX-Request guard other
  partials got; receipts missing the shared stale banner; pyproject.toml
  runtime deps a stale subset of requirements.txt.
- **Refuted (do not re-report)**: transactions-fetcher "no empty floor" — the
  real validator catches it; the finder read only the freshness gate.

### 003 completion note

Implemented directly by Fable. Consumer check confirmed the `dd_*` keys are read
only from the rank_v1 internal `context_only` (coverage/calibration audits), never
from the public snapshot — so dropped `dd_dynasty_rank`/`dd_dynasty_value`/
`dd_prospect_rank`/`has_dd_context`/`value_history_points` from the public snapshot
`context` (build_public_dynasty_snapshot.py); kept `source_ranks` (feeds the labeled
external-board panel). rank_v1 untouched, so its audit consumers are unaffected.
External-board panel note tightened to "External board context — not used in
ValuCast score"; confirmed `source_ranks` is never referenced in app.py (not on share
graphics). Verified: full suite 1312 pass, ruff clean.

### 002 completion note

Implemented directly by Fable (Codex still out). Decision 1 applied in full:
`_select_dynasty_store`/`_select_buy_source` never return DD — they serve a ready
ValuCast snapshot, a stale-but-valid one (`valucast_public_snapshot_stale`, ≤7d,
labeled via the index.html banner), or an explicit unavailable null-object. The
boot guard now refuses startup only when no ValuCast snapshot is servable
(`dynasty_data_source == "unavailable"`); `VALUCAST_REQUIRE_DD` removed from app.py
and render.yaml; `/health/ready` no longer gates on the DD feed (demoted to an
informational `dd_comparison_feed`); the DD feed dropped from the hard freshness
gate. DD feed sync stays (comparison-only). The legacy DD-shaped buy-board fallback
was removed (it would have run on snapshot rows) → buys are ValuCast-or-unavailable.
Verified: `/health/ready` 200 + ready with no DD; full suite 1311 pass; ruff clean.

### 001 completion note

Implemented directly by Fable (Codex unavailable — usage limit). Steps 1, 2, 4
shipped as written. **Step 3 (ratchet) adapted, honestly:** the committed data
artifacts predate `c8d8a22`'s provenance code, so `dd_factual_fallback` isn't
emitted into them yet and no real baseline could be captured. The ratchet
(`scripts/validate_dd_independence_ratchet.py`) reads
`valucast_prospect_rank_v1.json:validation.dd_factual_fallback` (NOT the public
snapshot — it doesn't carry that field), stays **dormant** (passes) when the field
is absent, and **self-seeds** its baseline (`data/models/dd_independence_baseline.json`)
from the first real daily build that emits the counts. No baseline file shipped yet
— it appears on the first build that carries the counts, then enforces ≤ ceiling.

## Batch 3 — 2026-07-09 claims-layer audit + receipts epistemics (planned at `fb360066`)

Source: the 10-surface claims-layer audit (`docs/audit-2026-07-09-claims-register.json`,
180 claims, 25 high / 31 medium / 13 low) plus the receipts scoring-philosophy review
(arrival-not-outcome, survivorship bias, seed asymmetry, unweighted lead time). The
at-promotion standard, the no-claim bucket, and the page disclaimer already shipped
directly (`fb360066`); these plans are the rest.

| Plan | Title | Priority | Effort | Depends on | Execution window | Status |
|------|-------|----------|--------|------------|------------------|--------|
| 010  | Single source of truth for contradicting chips (High+Thin, form spark, AOTC chip vs PNG, strip level) | P1 | M | 011 first (shared `_confidence`) | pre-flip | DONE (opus executor + Fable review, 7/9; card==PNG verified live P#3/~#66/+63; review added board_count>=3 chip floor; Form Curve→ValuCast Value Trend; 1785 pass) |
| 011  | Label honesty (confidence sample gate, 1-board consensus, ETA window, seed field_label, value-scale explainer, dead elite floor) | P1 | M | — | pre-flip | DONE (opus executor + Fable review, 7/9; did BOTH 4a derive + 4b validator — validator caught Watson seeded "outside top 100" vs a public board's #90, artifact regenerated with honest labels; elite floor 140→95, 4 clear; 1781 pass) |
| 012  | Binding gates (prospects readiness banner, receipts-PNG disclaimer parity, ledger Wins tile split) | P1 | M | — | pre-flip | DONE (opus executor + Fable review, 7/9; banner LIVE today — prospects gate is False (pitcher-heavy), self-clears when the gate passes; PNG caveat in header band; Wins tile → "Leading: 8 final · 23 trending"; 1788 pass) |
| 013  | Comps cohort + buys/movers window honesty (incl. the INERT window pills bug) | P2 | M | — | anytime | DONE (opus executor + Fable review, 7/9; pills verified live — 33/40 rows differ 7d vs 30d, era floor holds; share-card PNG stays canonical 14d; movers cap fineprint split two-sided; 1 test-drift assertion updated) |
| 014  | Methodology honesty polish (correlation coin-flip line, n= and noise caveats, forward-gate paragraph) | P2 | S | — | anytime | DONE (opus executor + Fable review, 7/9; win-rate = near coin-flip disclosed, n= on both ledger rows, live forward-gate loss shown (1.21x vs Steamer, 21% higher, sample still <30d)) |
| 015  | Scouting-read word-form number guard (2.09% measured flag rate) | P2 | S | — | anytime | DONE (opus executor + Fable review, 7/9; flag rate confirmed exactly 2.09% (15/716), known false-positive shapes pinned in tests; gates ok not hard_ok — regen candidate, not fact hazard) |
| 016  | Receipts maturation layer (pending → confirmed/decayed, frozen thresholds) | P1 | M | — | post-7/13 | DONE 2026-07-14 |
| 017  | Gaps → ledger claim lifecycle (kills survivorship bias; enrolls full qualified sets, not the 15-row display cut) | P1 | M | coordinate w/ 004 validator surface | post-7/13 | DONE 2026-07-14 |
| 018  | Lead time on every receipt row (omit-never-zero, archive-provable) | P2 | S | — | anytime | DONE (opus executor + Fable review, 7/9; flagged_days_early on 7/8 rows (Murphy 15d, Lara 24d), Hughes honestly omits (unprovable run); artifact regenerated additive-only; 1802 pass) |
| 019  | Field-unranked auto lane (replaces hand-seeding; data-derived field labels) | P2 | M | 018 helpful first (shared receipts surfaces) | anytime | DONE (opus executor + Fable review, 7/9; Hughes seed→auto verified; precedence scored>field-unranked>seed replaces the collision assert (2 live collisions, plan's own prose); launch guard binds on the new lane (+3 pre-launch excluded); review fix: page+PNG now display actual_call_up_date preferentially) |
| 020  | /gaps board-source freshness + consensus dispersion (2 HIGH + 1 MED register gaps no earlier plan absorbed) | P1 | M | — (land before 017; disjoint regions, same files) | anytime | DONE (opus executor + Fable review, 7/9; vintage resolved honestly (min 6/24, mtime trap avoided), dedicated staleness notice fires today; dispersion = anonymous min/max only, ToS intact) |
| 021  | Scouting grounding rounding: IP/AB precision leak ("182.888 innings"; real hook is mlb_read.py, not the register's repository.py guess; 7 dirty reads heal on next scheduled regen) | P2 | S | — (015's scoped-out upstream fix) | anytime | DONE (opus executor + Fable review, 7/9; Skenes 182.888→182.9 on the live path, 7 dirty reads documented — heal on next scheduled scouting build, no regen triggered) |
| 022b | (unplanned, Alex 7/9) No-claim disclosure + field-unranked BEHIND mirror lane | P1 | M | 019 | anytime | DONE (opus executor + Fable review, 7/10 UTC; all 22 no-claims listed collapsed w/ near-notes (Bratt/Gastelum/Johnson miss-shape, Cavanaugh/Ullola hit-shape); mirror lane symmetric to 019 — found Eldridge (1 board #20 vs VC #231), launch guard correctly excluded him, board stays 8/0; 1829 pass) |
| 022  | Trade Analyzer V1: free /trade verdict page from served values (Jim's-question-as-product; noise-band honesty, consolidation note, cross-universe disclosure; PNG = cut-to-M lever w/ mandatory cache-key add) | P2 | M | — | anytime | DONE (opus executor + Fable review, 7/10; PNG SHIPPED w/ give/get cache-key + 3 regression tests; all 3 honesty features render-verified with real players; footer link only, nav tab = Alex's call; 1871 pass) |
| 023  | Plate-Discipline Data Layer: pitch-level Swing/Whiff/SwStr (exact, ProspectSavant-matched) + Chase/Z-Swing/Z-Contact (pixel-calibrated ESTIMATES, tagged) from StatsAPI play-by-play; per-level cohort percentiles w/ hard min-pitch gate; new prospect-card bars + methodology; zone group = cut-to-M lever; EV forbidden; cards-only (no board/PNG) | P2 | L | — | post-7/13 | CODE COMPLETE + REVIEW FIXES (executor, 7/10; commits 176494e0→d5ef3e4b, fix batch 598830f4→4c4f1f3f): full scope incl. zone group; 16 review findings fixed (cold-cache/budget/failure-tolerance/tiny-guard CI safety, guarded YAML add, honest train/held calibration w/ per-pair sz — smoke r2=0.979, held-out agreement 99.6%>85% bar — non-Final feed guard, per-pitch zone_estimated, Swinging Pitchout whiff, reader schema guards, un-skippable app tests). Sirota AA Swing% 37.4/SwStr% 9.2 match spike. Cache stays UNCOMMITTED (~350MB full scale, gitignored). AWAITING REVIEWER: full ~8k-game backfill + artifact commit + full-suite gate |
| 024  | Model Verdicts registry (`/models`): public page listing every ValuCast model/subsystem w/ an honest four-way verdict (VALIDATED / PROVISIONAL / DEPRECATED / REJECTED), each row citing a committed evidence artifact a reader can open; hand-maintained registry JSON + build-time missing-evidence/overclaim validator; published failures (forward-gate losing ~1.21x vs Steamer, untrainable W target, gate-rejected usage features) are mandatory first-class rows; second accountability layer next to the call-level ledgers (/ledger, /receipts) — two-layer accountability nobody else in the space has; footer + board-tab placement, methodology cross-link; coverage lock = cut-to-S lever, missing-evidence gate NON-cuttable; display-only (no model/scoring/PNG touched, AOTC scoring files read-only); NO failure number hardcoded (link the live artifact — MEMORY's 1.21x/1.44x already stale) | P2 | M | — | post-7/13 | AUTHORED (Fable, 7/11; planned at f60b8545). Hand-authored, machine-validated: verdict is editorial judgment (a shadow_only artifact is PROVISIONAL, never auto-VALIDATED); validator fails the daily build if any cited evidence path is missing or a VALIDATED row cites a not-yet-proven artifact. Inventory verified against live data/models/*.json (all carry generated_at + status). Usage-features row CONDITIONAL — included only if a committed artifact substantiates it, else omitted |
| 025  | DSL / complex-league (FCL/ACL) coverage — SPIKE-FIRST w/ a written DECISION GATE: Phase A = feasibility memo (probe StatsAPI sportId 16/17 rosters+stats+ids, PA distributions for 17yo cohort, board coverage ≈0, mlbam resolvability %, twin-collision rate); Phase B = GATED minimal build only on PASS, defaulting to NO-value-number / NO-rank / NO-board — stat lines + age-vs-level + watchlist + "too early to price"; separate stat-only artifact off the universe/consensus/AOTC path; base-rate/survivorship claims forbidden | P3 | A:S / B:M-L (gated) | — | post-7/13 | AUTHORED (Fable, 7/11; planned at f60b8545). Two-phase, spike-first: Phase A ships a memo + gate verdict, Phase B runs only if all 5 gate criteria pass (>95% mlbam resolvability, rosters served, collisions manageable, honest sample floor exists, honesty holds under empty consensus). Honesty frame IS the product — the honest surface is deliberately small |
| 026  | Board Time Machine: public `/board/<date>` historical board replay from the already-committed daily `valucast_prospect_rank_v1` archives; fail-soft reader + thin server-rendered template + date picker; per-date quality flags (clean / pre-baseline < 6/22 role-norm epoch / unavailable) so contaminated states are DISCLOSED not hidden; as-of banner + today link + methodology; aggregate median + board count consensus ONLY (ToS, historically too); network-free, NO new build step/artifact/PNG; consensus column = cut-to-S lever | P2 | S–M | — | post-7/13 | **DONE (executed 7/17 vs base 13eacb6a; impl + 2 adversarial reviews, both SHIP no-fixes)**: read-per-request + mtime-keyed LRU(8), nightly-index alternative NOT built (measured cold parse 0.12s on a 12MB file — archives grew past the plan's ~7MB); consensus column SHIPPED aggregate-only (0 per-source leaks across all 34 rendered dates); epoch boundary follows the import — now the 7/14 gate-fix re-baseline, so 31/34 dates flag pre-baseline (honest per single-boundary contract); FAST-FOLLOW registered: epoch-history list so pre-6/22 vs 6/22–7/13 eras are distinguished; full suite 2380 green. Originally AUTHORED (Fable, 7/11; plan 026). Grounded in live archive read: rank_v1 archive = 29 dates 6/13→7/11, ~7MB/file, raw model-artifact shape (score/mlb_team/context_only.source_ranks, NO id/value/team → live template NOT reusable, needs thin adapter). Contamination story corrected from memory: NO literal date denylist in-repo; 6/2/6/10 masking is DD-side (irrelevant to this archive); real boundary = importable `prospects.buys.PROSPECT_BUYS_EPOCH` (6/22), so 6/13–6/21 = pre-baseline. Reuse: StatcastStore/PitchDisciplineStore (fail-soft), public_snapshot_models consensus median (aggregate-only), editorial_date (as-of). Frozen ahead_of_consensus.py = READ-ONLY reference for archive-read idiom, never edited/imported |
| 028  | Fix the prospect model's **pitcher lean** (model-surgery, L): the governor blocks the public board as pitcher-heavy (top-25 11 P vs cap 7) and the 7/13 memo verdicts it a **model defect** — pedigree over-weighting on the model's weakest axis (pitcher rank-concordance 0.49 vs hitter 0.78 in its own backtest), a peak layer that can't mark down (negative_delta_count=0 in all 14 buckets), zero pitcher-attrition discount, and two live stale-pedigree bugs (Hughes #10 on a 2022 slot ~consensus #571; Murphy #16 on ~20 IP). THREE fixes, strict priority, cut line bottom-up: **(a)** stale-pedigree decay/cap that BINDS on the `prospect_model_v0_6` MAIN path (the existing cap is fallback-only — Hughes/Murphy have a model_score so they bypass it; the pedigree features `pick_value`/inverse-draft/log-bonus flow into the outcome head uncapped+undecayed at `model.py:383-386`; `_pedigree_spare_credit` only decays a *confidence haircut*, not the point estimate) + make the governor guardrail bind (it filters on `score_source==PEDIGREE_SCORE_SOURCE` so it's blind to main-path pedigree, reports 0.0) — MUST SHIP; **(b)** pitcher outcome/impact concordance-gap down-weight + a role-based pitcher attrition base-rate discount in `availability.py` (none exists today) — ships with (a); **(c)** blend decorrelation (outcome+impact share one feature vector, r=0.92 elite pitchers; `max()`-over-role-shape impact target) — CUTTABLE, retrain-IS-validation. GATE: rerun the outcome backtest (rebuild upstream adapter+dynasty+forward FIRST, then re-aggregate) — pitcher concordance ↑, hitter not materially ↓; Anderson/Hernandez/Sloan (consensus-supported arms) stay top-25; NEVER hand-tune to exactly 7 (tie-noise, banner MAY stay up ~9 P — acceptable); bump `PROSPECT_BUYS_EPOCH` to mask the re-baseline; AOTC integrity = frozen scoring-logic zero-diff (AOTC is rank-coupled so the funnel IS expected to move — assert logic unchanged, not artifact identity). Adams/Jenkins routing-cliff = hitter-tail artifact, NOT motivation | P1 | L | — | **CLOSED** | TERMINAL (executed 7/13-15; do NOT re-open): fix-(a) decay BUILT dark (model.py:436-439, PITCHER_STALE_PEDIGREE_DECAY_ENABLED=False) but its ONE registered look (C1, seed 28013) was SPENT 7/13 and FAILED (delta_c_pitcher +0.00067 vs +0.005 floor, LB -0.0048; commit 6aacea90) — flag never flips without a NEW-VINTAGE gate (2022 cohort clears the 4yr horizon post-2026 season); fix-(b) cut on power (0.261 vs 0.70 floor, veto decision plans/028 file:1457-1476), attrition discount never built; epoch bump shipped a27df837. Live symptoms (Mozzicato/Hughes pedigree ranks, top-25 ~10 P, display-only banner) = the INTENDED honest state. Originally AUTHORED (Fable, 7/11; planned at 125cd43c). Evidence base = `docs/governor-review-2026-07-13-pitcher-lean-memo.md`, every claim re-verified in-code at HEAD: the fallback-only cap (`rank_v1.py:958-1023` reached only via the `elif` at :1098), the main-path branch (`_score_components:1087-1096`, model_score 0.76), the pitcher pedigree features (`model.py:383-386`), the 0.58/0.42 blend (`rank_v1.py:821-844`), the governor's source-keyed pedigree checks (`valucast_governor.py:571-752`, blind to `prospect_model_v0_6`), the role-shape block (`:436-489`, thresholds imported from `calibration_report.py:31-32`), the gate metrics (`evidence.adapter_fixed_horizon.roles[0]=hitter 0.779 / [1]=pitcher 0.490`), and the rank-coupled AOTC funnel (scorecard replays the archive through rank-gated guards → moves with a re-score by design). Model-surgery, HIGH risk; reviewer reruns the full upstream backtest chain + suite + frozen-file diff and gates the push |
| 029  | Plate-Discipline Leaders: public server-rendered `/discipline-leaders` board over the SHIPPED 023 artifact (1,322 players; competitive origin = ProspectsLive generic stat-leaders 7/12 — below-AAA discipline renderable by NOBODY else; second job = self-refreshing social post material). V1 = SEASON-TO-DATE ONLY (7d/30d windows need the UNCOMMITTED pitch cache — 023's 2b decision NOT reopened; explicit fast-follow tied to discipline CI-refresh); only the artifact's 4 oriented metrics get leader framing (Chase%/Whiff%/SwStr% low-good asc, Z-Contact% high-good desc; Swing%/Zone%/Z-Swing% contextual = NO board); qualifies-only ranking (min 300 pitches stated on-page, sub-floor NEVER appears), per-level never blended, est. tag per-bucket `zone_estimated` (mixed measured/estimated levels are disclosed row by row + methodology link, `.pd-est-tag` reuse); name join via generation-memoized dd_store mlbam map (artifact has NO names; unresolved rows stay ranked w/ fallback label); slugged params (?level=a-plus — the "A+"-decodes-as-space trap) validated vs hardcoded allowlist maps; footer + cards-gallery linkage, NO nav tab; observe-only (no value/rank column); PNG share card = cut-to-S lever, but if shipped `level`/`metric` MUST enter `_PNG_CACHE_PARAMS` + the 3-test cache-key trio (007/022 invariant) | P3 | S-M | 023 (SHIPPED 7/10) | post-7/13, AFTER the current stack (discretionary) | IMPLEMENTED on `codex/discipline-leaders` (7/27; review pending). The board, PNG, disclosures, cache-key isolation, empty-state behavior, footer/gallery links, and full regression suite are complete. Launch verification found the existing daily refresh always cold-skipped because its raw event cache was not persisted; the branch therefore includes a pinned restore/bootstrap/save cache repair as a documented operational deviation from the original no-YAML scope. |
| 030  | The Forward Ledger: pre-registered, commit-dated forward accuracy benchmark (registration doc + S1 cohort registry + S2 scoreboard roll-up). ValuCast scored against the aggregate public consensus via the Anticipation Score (median signed lead + bootstrap 95% CI + sign-permutation null-interval context; NO p-value — degenerate for low-magnitude-variance pools, removed pre-freeze); inherits the frozen gaps claim ledger by REDECLARATION never import (roll-up only); one-look quarantine (no `role`/pitcher-hitter split, validator-asserted); no per-source rank in any emitted artifact (consensus median + board_count only); no back-dating (registers TODAY off the latest committed archive). S1+S2 = new `prospects/forward_cohort.py` + `prospects/forward_scoreboard.py` + 2 build scripts + validator + tests; S3 CI wiring + S4 public `/scoreboard` page are separate sessions. Filename registered as plan 030; the 029 slot was already held by plate-discipline-leaders | P2 | M | — | post-7/13 | REGISTERED + SHIPPED (7/16): cohort #1 registered 2026-07-16 off the 7/16 archive (515 eligible, 461 consensus-covered, 54 rank-only, sha256-stamped); live scoreboard artifact built + validated (pool 59 = 31W/28L, median +2.0d, CI (-6,+13), provisional, self-retraction 105/238); S3 nightly wiring live (build+validate+guarded git-add; registry build asserted ABSENT from nightly); S4 `/scoreboard` + share-card PNG shipped DARK behind SCOREBOARD_HOLD (flip after 2 clean nightlies), all 7 anti-trophy items rendered from artifact |
| 027  | Glossary + number-resolution discipline (eephus's convention, ENFORCED): hand-authored `data/manual/valucast_glossary.json` (20-40 real terms, each w/ id/term/definition/origin/optional-formula/see_also + an OPTIONAL real worked example traced to a committed artifact w/ an `as_of` — never invented) rendered at `/glossary` (or a `#glossary` methodology section — Step 1 decides); the every-number-resolves CI check = a fail-HARD `validate_label_glossary_coverage.py` that unions the importable label dicts (prospect_percentiles.METRIC_LABELS, pitch_discipline_store._DEFAULT_LABELS, category registry) + a curated MAJOR_SURFACE_LABELS registry and fails the build on any unresolved label (visible EXEMPTIONS, never DOM-scan, coverage ratchets from card/trade/boards); glossary-anchored tooltips = the existing `(?)`/`est.` `<a href title=>` pattern pointed at `/glossary#<id>`, server-rendered zero-JS = cut-to-S lever; dated methodology CHANGELOG seeded w/ git-verified real dates (plate-discipline 7/10, forward-gate 7/9, AOTC scorecard v2 7/2, SV/HLD split 7/1 — NO ClaudeBot max_tokens/exit content, that's a different project); named DISPLAY PRINCIPLE ("absent→dash never fabricated; estimate→labeled") elevated to top of /methodology + verified TRUE against live surfaces (est. tag, &mdash; dash, sub-floor no-bar already exist at methodology.html:387-398). Consensus terms = aggregate median + board count ONLY (ToS); composes-with-not-duplicates plan 024 (/models cross-link conditional on the route existing) | P2 | M | — | post-7/13 | **IMPLEMENTED ON `codex/glossary-number-discipline` (2026-07-26; awaiting integration review).** Standalone fail-soft `/glossary`; 40 validated terms; 64 enforced labels across cards/trade/boards; methodology principle + append-only history; footer and contextual help links; both validators wired into the daily validate path. No model, valuation, rank, PNG, or publication-governor change. |
| 031  | Pitcher Strike% gate (Wave B): `strike_pct_only` challenger registered off exploratory dry-run 31017 (pitcher Δc point +0.005677 / 98.33% LB +0.001653 — the only lever in either wave with a live gate path); fitted level-translation lever explicitly NOT registered (negative both axes — additive per-level shifts absorbed by the ridge; reopening needs a structurally different form); dual gate = registered C1 Δc_pitcher form + anchored Brier non-inferiority guard (−1.0% = ½ MIN_GATE_IMPROVEMENT_PCT, data-informed change disclosed); dual data pin (contract + raw dataset, git-based) | P1 | done (dark) | d10cfd85 base | — | **REGISTERED 2026-07-16, LOOK HELD (DO-NOT-SPEND)**: code committed dark (both flags False, serving byte-identical, 2287+17 pass, 3 adversarial reviews: 2× SHIP-AFTER-FIXES + math DO-NOT-SPEND); look outcome already deterministic (LB ~58σ from flipping) and effect is 2021-fold-concentrated (~22% null-pass under cohort noise; every positive pitcher lever in both waves is 2021-dominated); seed 31013 NEVER executed — re-arm = post-2026-season new-vintage epoch (2022 cohort = virgin 4th fold, SUPERSEDING 4-fold registration w/ fresh seed; do not spend the 3-fold look even then; same batch as 028-C1/E1 re-tests) |
| 032  | Competition Proof Lane: immutable, outcome-grounded head-to-head cohorts against a public prospect-board baseline and a public pitcher-skill baseline; identical players/cutoffs/horizons, percentile-rank MAE + player-clustered 95% CI, pairwise concordance and top-k regret; scoped claims only, no universal “better” language; comparison-only and forbidden from rank/value/Role Watch/pitcher cap/publication | P1 | S | Plan 030 conventions | post-7/19 | **REGISTERED + DARK (2026-07-19)**: the prospect cohort freezes 2,611 matched players; the pitcher cohort freezes 12/12 published starters; both honestly `collecting` with 0 matured outcomes. A separate underpowered 91-player public-board historical replay has an underlying `collecting` state under the 150-player floor and remains `research_only`; no claim is authorized and the public artifact stays empty. |
| 033  | Normalized-Production Prospect Research Gate: one immutable, research-only Control-versus-normalized-production look using cohort-season-completion inputs, separate hitter/pitcher results, and in-look subset/component reports | P1 | S | Tasks 2–4 + Review checkpoint B | post-7/20 | **SPENT — INVALID FOR ADJUDICATION / DESCRIPTIVE ONLY (2026-07-20)**: post-run audit found three defects: `[0,1]` quantiles entered raw-unit model/Rank-v1 guards; partial-impact references leaked from the full outcome store instead of being frozen per fold from training only; and current/AAA ID-only joins collapsed two-way roles, making sealed AAA missingness and role counts order-dependent. The sealed role outputs, partial-impact numbers, and AAA counts are not evidence for or against normalized production and were not rewritten. The sealed correction record binds both Task 6 hashes and keeps rerun/claims false. A future look needs a fresh registration fixing all three classes; there is no free rerun and no live model, rank, artifact, or workflow changed. |
| 034  | Post-2026 Prospect Challenger Epoch: nested training-fold selection of one role-specific composite from the registered ridge/baseline/stacking/investment/target/shrinkage/confidence families; untouched outer cohorts, fold-local impact references, exact mlbam_id:role identities, and direct realized-value readiness required before any promotion review; buy-momentum and genuine cross-universe mapping reserved as separate decision tracks | P1 | L | 033 post-run audit + fold-local impact audit | not before 2027-01-01 | **PROTOCOL REGISTERED — WAITING FOR 2026 VINTAGE; NO LOOK AUTHORIZED (2026-07-21)**: seeds 34021/34027/34031 reserved and fresh; expected outer folds 2018/2019/2021/2022; exact grids, code/data hashes, and implementation paths must be frozen in one reviewed pre-look amendment. No model, rank, value, workflow, publication rule, or public claim changed. |
| 035  | MLB Pitcher Skill Challenger: one locked residual-rate challenger over the frozen Control, with a single retrospective look and research-only ablations | P1 | L | — | research-only | **REGISTERED — UNSPENT; RESEARCH ONLY** |

Ordering: **011 → 010 → 012** sequential (shared `_confidence` + board template), then
013/014/015 independent; receipts cluster **018 → 019** then **016 → 017** post-7/13
(017 also depends on the AOTC unlock; both forbid touching frozen scorecard files).
**023** (plate-discipline data layer) is independent of the receipts cluster and also
executes post-7/13; it is effort-L and self-contained (new build script + committed
artifact + fail-soft reader + card section), so it slots in whenever there is a full
session for it after the 7/13 unlock — order it after the receipts cluster if hands
are limited. **024** (Model Verdicts registry) is independent, post-7/13, and effort-M;
it is display-only (a hand-authored registry JSON + a fail-soft `/models` route + a
build-time validator) and reads existing committed artifacts read-only — no model,
network, or pipeline math — so it slots in anytime after the 7/13 unlock; it reads the
AOTC scorecard as evidence but never touches the frozen AOTC scoring files. **025**
(DSL/complex coverage) is also independent and post-7/13, but it is **spike-first with
a hard decision gate**: its Phase A is a cheap (S) feasibility memo, and Phase B (the
M–L build) runs ONLY if the memo's 5 gate criteria pass — so the honest first
deliverable may be "memo, gate failed, STOP," which is a success. Run 025 Phase A
whenever there is an hour for API probing; do not schedule Phase B until the gate
verdict is in. **026** (Board Time Machine) is independent, post-7/13, and effort-S–M;
it is a pure new read surface over archives the daily pipeline ALREADY commits — a
fail-soft `/board/<date>` reader + a thin server-rendered template — so it adds no
network, no build step, and no new artifact (default path), and never touches the
live board, the value/scoring math, or the frozen AOTC files (it only READS the
frozen `ahead_of_consensus.py` as a reference for the archive-read idiom). It slots
in anytime after the 7/13 unlock; the per-row consensus column is its cut-to-S lever.
**027** (Glossary + number-resolution discipline) is independent, post-7/13, and
effort-M; it is display/copy-only — a hand-authored `data/manual/valucast_glossary.json`
+ a fail-soft `/glossary` render + TWO validators (schema/provenance + the fail-HARD
every-number-resolves coverage gate) + a named display principle and a dated changelog
on `/methodology` — and reads existing committed artifacts read-only as *example
sources* (no model, network, or pipeline math), so it slots in anytime after the 7/13
unlock. Its enforcement (the coverage check fails the build, never warns) is the whole
point vs eephus's convention-only glossary and is NON-cuttable; the glossary-anchored
tooltips are its cut-to-S lever. It **composes with plan 024 without duplicating it**:
the glossary defines vocabulary, the (AUTHORED-not-yet-shipped) Model Verdicts registry
judges models — every `/models` cross-link is conditional on that route existing, and a
glossary term never restates a verdict or a failure number. Like 026 it never touches
the value/scoring math or the frozen AOTC scoring files (it only READS the committed
scorecard as an example source). Consensus terms stay aggregate-median-+-board-count
only (ToS), and no tooltip may add a share-card PNG query param (`_PNG_CACHE_PARAMS`).
**028** (pitcher-lean model fix) is the outlier of the batch: unlike 024-027 (all
display/copy-only) it is **model surgery** — the highest-priority (P1) correctness fix
here, and the direct action item from the 7/13 governor review. It is **STRICTLY
post-7/13 and gated behind the AOTC unlock** (it touches the rank artifact the frozen
AOTC scorecard reads), and it is the one plan that deliberately re-scores the live
board, so it carries HIGH risk and a hard acceptance gate (backtest concordance +
consensus survival, never a hand-tuned pitcher count). It does NOT depend on 016/017
(the post-7/13 receipts cluster) but shares their AOTC-unlock timing and their
"never touch the frozen scorecard files" invariant; it **composes with the separate
~7/13 buys-momentum re-baseline-masking fix** (public commitment) via the
`PROSPECT_BUYS_EPOCH` bump — 028 must not depend on it but must not conflict. Run 028
in a full dedicated session after the unlock; if hands are limited, it can trail the
receipts cluster. Like the others it never relaxes the governor thresholds and never
publishes per-source ranks (consensus-alignment reads `context_only.source_ranks`
internally only). **029** (Plate-Discipline Leaders) is the leaderboard fast-follow
plan 023's Non-goals explicitly anticipated, now that the artifact is proven (shipped
7/10, live): a pure read surface (thin store extension + one route + one template)
over the committed discipline artifact — no new build step, artifact, or network. It
is P3/S-M and **discretionary: schedule it AFTER the current post-7/13 stack**
(receipts cluster, 028, and the other authored plans take precedence); it slots into
a spare half-session. It depends only on 023 (shipped), conflicts with nothing (028
touches the rank artifact, never the discipline artifact; 027 imports the store's
`_DEFAULT_LABELS`, which 029 must not rename), and its PNG share card is the cut-to-S
lever — if the PNG ships, `level`/`metric` enter `_PNG_CACHE_PARAMS` with the
mandatory cache-key regression trio. Every plan: master only, do NOT push, reviewer gates. The
receipts public flip (RECEIPTS_HOLD app.py:78) is Alex's call and is expected after
011/012 land.

### Register triage 7/9 (so nobody re-audits the leftovers)

All 69 register gaps mapped against plans 010-021: 55 absorbed, 14 leftovers triaged.
- **Fixed direct (8801cb5c):** multi-level `level_label` "+"-join ("A++A" class, HIGH) — now " & ".
- **Fix-now queue (apply after 011 lands; its files):** positional share-card dangling `· {date_label}` separators (app.py:6515/6535); /map tooltip `toFixed(1)` → whole; board Dynasty Value `%.1f` → whole; scouting Source-line freshness hedge (player_detail_dynasty.html:584-587).
- **Planned:** 020 (gaps freshness/dispersion), 021 (grounding rounding).
- **Accepted (defensible/documented, do not re-report):** methodology headline scoping + opt-in caption (014 defers, by design); percentile bars pool MiLB levels + per-card confidence-floor divergence (documented in code); auction-$ model knobs (honest labels, internally consistent); movers "why" season-line + 7d/2d slope mix (fineprint discloses); scouting exact-hash reuse within 10-pt percentile bucket (3 guards already).
- **Post-7/13 plan candidate (not authored):** /map cross-universe axis = two separate 0-100 normalizations relabeled as one scale (`value_mutation:"none"`) — touches the value pipeline; pair with the model-gated valuation findings at the AOTC unlock.

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Dependency notes

- **001 before 002.** 001 installs the guards that make a silent DD regression
  impossible; 002 is the behavioral change those guards protect. 001's positive
  route assertion (Step 4) is written against a ValuCast-source *allowlist* that
  already includes the `valucast_public_snapshot_stale` value 002 introduces, so
  the two do not fight.
- 003 is independent and can run any time, but is cleaner once 001 exists.

## Findings considered and deferred / rejected

- **Own `mlb_stat_line` (the last DISPLAY-FALLBACK keeping `factual_path_fully_valucast_owned` False):**
  deferred, not planned here. There is no owned MLB-actuals source yet — owning it
  is a *build* (pull a current-season line from the MLB Stats API / an owned cache),
  not a *cut*. Plan 001's ratchet ensures the count can't grow meanwhile; tackle the
  build as its own plan when ready.
- **Delete `scripts/sync_dd_prospect_inputs.py` + `data/dd/prospect_model_inputs.json`:**
  deferred. It is off the live workflow (tests assert that), but it is still imported
  and exercised by ~8 tests in `tests/test_public_data_refresh.py` and its output
  path is referenced by `docs/prospect-model.md` and `docs/universal-prospect-model.md`
  as the model's fingerprint contract. Removal is a deprecation with real blast
  radius (tests + docs + possibly the runner's fingerprint path), not a P0 cut.
- **Remove the dead `value_history` sparkline render path:** deferred (very low
  value). It renders nothing under the snapshot (the field is DD-only and never
  populated), so it is not a trust/independence risk — just dead code. It is also
  the stub of an *unshipped* ValuCast value-trend sparkline feature; deleting it
  silently drops that feature. Decide delete-vs-wire-ValuCast-data as part of the
  value-trend feature, not this cleanup.

## How to execute

These plans are written for Codex (the executor per the Fable-plans / Codex-executes
split). Hand one plan at a time to Codex via the `codex:codex-rescue` agent, then
Fable reviews the diff against the plan's Done criteria + runs verification before
accepting. Do them in order 001 → 002, with 003 whenever convenient.
